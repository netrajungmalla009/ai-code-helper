from src.core.config import settings

print(settings.app_name)

print(settings.environment)

print(settings.is_development)

print(settings.default_llm_provider)

print(settings.default_llm_model)

print(settings.enable_openai)

print(settings.enable_anthropic)

print(settings.enable_ollama)

print(settings.ollama_base_url)
